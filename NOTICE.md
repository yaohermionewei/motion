# 第三方许可说明

## Motion Index 原创内容

以下内容由 Motion Index 项目独立实现，并适用根目录 `LICENSE` 中的 MIT License：

- `src/` 中进入正式目录的组件浏览器与 22 个动效组件。
- `public/motion-index-mark.svg`。
- `scripts/` 中的公开结构检查与部署边界检查。

仓库不包含来源网站的生产源码、恢复运行时、品牌页面、模型、原始动画文件或研究截图。

`public/images/` 中的 PNG 只作为网站演示内容，不属于可复制组件代码的 MIT 许可范围。使用者应在自己的项目中替换为自有图片。

## 字体

- DM Sans：SIL Open Font License 1.1，见 `public/fonts/OFL-DM-Sans.txt`。
- Instrument Serif：SIL Open Font License 1.1，见 `public/fonts/OFL-Instrument-Serif.txt`。
- Space Mono：SIL Open Font License 1.1，见 `public/fonts/OFL-Space-Mono.txt`。
- Syne：SIL Open Font License 1.1，见 `public/fonts/OFL-Syne.txt`。

## JavaScript 依赖

- React、React DOM、Three.js、Lenis 和 Lucide React 使用各自的上游开源许可。
- 依赖版本记录在 `package.json` 与 `package-lock.json` 中。

第三方许可不改变 Motion Index 原创代码的 MIT License，也不允许重新声明第三方字体或依赖的许可。
